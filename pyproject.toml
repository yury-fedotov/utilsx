[project]
name = "utilsx"
version = "0.0.1"
description = "Generic utilities for Python projects"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
