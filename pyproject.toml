[project]
name = "utilsx"
version = "0.0.1"
description = "Generic utilities for Python projects"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "mypy~=1.15.0",
    "pytest~=8.3.5",
    "ruff==0.11.8",
]
